import 'package:flutter/material.dart';

class HttpRequestErrorWidget extends StatelessWidget {
  const HttpRequestErrorWidget();

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.error_rounded);
  }
}